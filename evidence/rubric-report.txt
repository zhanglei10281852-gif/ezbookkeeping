项目：mayswind/ezbookkeeping｜题目：交易图片上传失败后的对象回滚｜Session 短 ID：6aa51fd5a4c3ab1fb985cb46
验证摘要：go test -count=1 ./... 全部通过；go build ./... 通过；go vet ./... 通过；git diff --check 通过。
Rubric ID：1｜状态：通过｜期望行为：图片对象写入成功但数据库插入失败时，上传流程返回稳定错误并清理或登记该对象以便后续恢复，不留下不可见孤儿。｜实际观察：UploadPicture 先写入 staging/{uid}/{pictureId}.{ext}，数据库事务插入失败时返回 insertErr，并只删除本次上传的暂存键；删除失败时保留暂存对象供维护任务识别。｜直接证据：pkg/services/transaction_pictures.go:220-241 的暂存写入、事务插入、定向删除和原错误返回路径。
Rubric ID：2｜状态：通过｜期望行为：图片记录提交成功后，交易读取能够在三种现有存储后端按记录扩展名得到同一对象。｜实际观察：记录提交成功后调用 MoveStagingTransactionPictureToFinal；读取缺少正式对象时检查暂存对象并移动到正式键后再次读取；local、MinIO、WebDAV 均实现 Move/List，完整 Go 套件通过。｜直接证据：pkg/services/transaction_pictures.go:244-251、368-399；pkg/storage/local_filesystem_storage.go:99-125；pkg/storage/minio_storage.go:100-151；pkg/storage/webdav_storage.go:256-343；go test -count=1 ./...。
Rubric ID：3｜状态：通过｜期望行为：重复提交、进程重启或清理重试不会删除仍被活动交易引用的图片，也不会生成重复可见记录。｜实际观察：活动记录在 staging 对账中只执行发布或删除冗余暂存对象，正式区扫描遇到未删除记录直接跳过；删除流程只软删交易引用并由唯一 pictureId 记录维护。｜直接证据：pkg/services/transaction_pictures.go:490-529、597-610；pkg/services/transactions.go:2069-2090；pkg/models/transaction_picture_info.go 的 picture_id 主键定义。
Rubric ID：4｜状态：不通过｜期望行为：对象删除暂时失败时，数据库状态和恢复标识保持一致，维护路径再次运行可完成清理并报告结果。｜实际观察：每批正好 200 条软删除记录且对象删除持续失败时，deleteTransactionPictureObjectsAndRecord 不改变记录；cleanupDeletedTransactionPictures 因 len(pictureInfos)==200 重复查询同一批，函数不会退出并且后续暂存区、正式区阶段不会执行。｜直接证据：pkg/services/transaction_pictures.go:431-463 中固定 Limit(200)、失败后记录保留且循环只按批大小判断退出；pkg/services/transaction_pictures.go:299-324 显示后续阶段依赖该函数返回。｜原因：整批删除失败时循环没有失败游标、单次处理上限或终止条件，持续失败会让维护任务卡在同一批记录。｜影响：对象删除失败不能在一次维护运行中稳定报告并进入后续恢复阶段，暂存对象和孤立对象的清理被阻断。｜证据位置：F:\swe\091201\project-03\pkg\services\transaction_pictures.go:431-463。
Rubric ID：5｜状态：通过｜期望行为：未启用对象存储或读取缺失对象时，既有上传响应、图片未找到和扩展名错误语义保持兼容。｜实际观察：未启用交易图片时存储容器保持 nil 且清理任务不注册；读取仍按原有记录不存在、扩展名不匹配和 os.IsNotExist 分别返回既有错误。｜直接证据：pkg/storage/storage_container.go:43-50、133-167；pkg/cron/cron_container.go:88-90；pkg/services/transaction_pictures.go:168-198、368-389；go test -count=1 ./...。
Rubric ID：6｜状态：通过｜期望行为：并发上传和交易删除交错发生时，图片引用关系、软删除状态及对象生命周期不会互相覆盖。｜实际观察：上传使用独立 staging 键，记录提交后才发布；交易删除只在同一 UserDataDB 事务中更新图片软删除字段；清理删除对象后才硬删记录，发布和删除均为幂等路径。｜直接证据：pkg/services/transaction_pictures.go:220-251、633-649；pkg/services/transactions.go:2011-2090；pkg/storage/local_filesystem_storage.go:99-125；go test -count=1 ./...。
